name: Sync Analyzer with Yulia core v3

on:
  push:
    paths:
      - 'patch-sync-analyzer-yulia-v3.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply shared-core patch
        run: python3 patch-sync-analyzer-yulia-v3.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analyzer/app.js js/engine/triples-chat.js analyzer/sw.js analyzer/index.html
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "fix: unify Analyzer and Yulia forecast core"
            git push
          fi
