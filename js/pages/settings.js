import {card} from "../ui.js";
export function renderSettings(ctx){
 return `<div class="grid cols-2">${card("ИСТОЧНИК ДАННЫХ",`<div class="list-row"><span>Источник</span><b>Столото TOP-3</b></div><div class="list-row"><span>Автопроверка</span><b>каждые 5 минут</b></div><div class="list-row"><span>Ручной ввод</span><b class="bad">ОТКЛЮЧЁН</b></div>`)}${card("О ПРИЛОЖЕНИИ",`<div class="list-row"><span>Название</span><b>TOP-3 CHAT MASTER</b></div><div class="list-row"><span>Версия</span><b>${ctx.version.version}</b></div><div class="list-row"><span>Ядро</span><b>chat-master-v1</b></div>`)}</div>
 ${card("ПРОГНОЗИРОВАНИЕ",`<p>Старые основные экранные методы TOP / Δ / ЧИСЛА / Допы / Сканер заменены. Основной прогноз формируется системой из чата: <b>MAIN TIME-DIRECTION + PERM→FAMILY Algorithm + скрытые APP CORE / MATRIX SHIFT → MASTER TOP-3</b>. Пользователю выводятся ровно 3 комбинации. Зеркало +15 <b>сохранено отдельным контуром тройников</b> и не входит в MASTER.</p>`)}
 ${card("NO-LEAKAGE",`<p>Прогноз фиксируется до нового факта. После факта приложение только проверяет frozen, записывает RAW-источники, family, поддержку цифр, связи PERM→FAMILY и работу над ошибками. Переписывать старый прогноз запрещено.</p>`)}
 ${card("ОБНОВЛЕНИЕ",`<p>Одна кнопка <b>«Обновить»</b> проверяет version.json, новый тираж и серверный frozen. GitHub Actions продолжает запускаться каждые 5 минут.</p>`)}`;
}
